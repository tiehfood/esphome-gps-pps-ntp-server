# ESP32-P4 Hardware-Timestamped NTP Probe — Implementation Plan

**Goal:** Build a measurement instrument that drops our NTP A/B noise floor from ±19 µs to
sub-microsecond, and gives a ±1–5 µs absolute reference, so firmware changes to the S3 NTP
server become measurable instead of unfalsifiable.

**Architecture:** A bare ESP-IDF app on a Waveshare ESP32-P4 ETH PoE. The P4's EMAC stamps
NTP request/reply frames in hardware at the SFD. The P4 stays dumb — it returns raw
`(T1,T2,T3,T4)` quadruples over UDP; all statistics, interleaving and A/B control live in
Python on the Mac Studio.

**Tech stack:** ESP-IDF 5.5.1, ESP32-P4 (rev 1.3), IP101 PHY over RMII, MCPWM capture,
Python 3 on the Mac for control/analysis.

---

## Global Constraints

- **ESP-IDF 5.5.1** — the version already on this machine (`~/.platformio/packages/framework-espidf`).
- **Silicon is rev 1.3** → `esp_eth_mac_set_pps_out_gpio()` is unavailable (needs rev ≥ 3).
  Every design choice below assumes no hardware PPS output.
- **No ESPHome** for the probe. Bare IDF app, separate build tree from `ntp_server.yaml`.
- **Must not disturb the S3 server, Home Assistant, the Pi, or the Mac Studio.** The probe is
  a client; the only write it performs against the S3 is the existing A/B switch over HTTP.
- Board pins (verified from Waveshare wiki + ESPHome P4 defaults):
  RMII data `34 TXD0, 35 TXD1, 28 RX_CRS_DV, 29 RXD0, 30 RXD1, 49 TX_EN`;
  `MDC 31`, `MDIO 52`, `PHY power 51`, `CLK_EXT_IN 50`, `phy_addr 1`.
- **No AI attestation** in commits (repo rule).

---

## Measurement Theory — why this works

**Round-trip delay is absolute with no shared clock.**

    δ = (T4 − T1) − (T3 − T2)

T1/T4 are P4 time, T2/T3 are S3 time; the clock *offsets* cancel algebraically. Only relative
*rate* error survives, and over a ~1 ms round trip that is picoseconds. So a free-running P4
measures δ to nanoseconds. This alone lets us compare measured delay against the physical
minimum (serialization + switch store-and-forward + PHY) and attribute the excess to device
latency.

**θ is differential without a reference.** θ = θ_true + c, where c is the P4's unknown clock
offset. In an interleaved A/B, c is constant across the window and cancels in the difference.

**θ becomes absolute (±1–5 µs) in Phase 7** by measuring the P4 PTP clock against GPS PPS.
That link crosses software, so it carries a constant bias equal to the inter-register read
skew. Mitigation: time back-to-back reads of each counter to measure the skew directly, and
alternate read order so the residual is variability rather than mean.

---

## Verified Facts (checked against local ESP-IDF 5.5.1 source, 2026-09-09)

| Fact | Evidence |
|---|---|
| `SOC_EMAC_IEEE1588V2_SUPPORTED (1)` for P4 | `components/soc/esp32p4/include/soc/soc_caps.h:800` |
| PTP ioctls: `PTP_ENABLE`, `S/G_PTP_TIME`, `ADJ_PTP_FREQ`, `ADJ_PTP_TIME`, `S_TARGET_TIME`, `S_TARGET_CB` | `esp_eth_mac_esp.h:192-201` |
| PTP code gated ONLY by the SOC cap — no `CONFIG_` flag needed | `esp_eth_mac_esp.c:307,456,627,871` |
| RX path carries `void *info` | `esp_eth_driver.h:82,303` |
| TX ctrl path `esp_eth_transmit_ctrl_vargs(hdl, ctrl, argc, ...)` | `esp_eth_driver.h:338` |
| MCPWM: 2 groups × 1 capture timer × 3 channels | `soc_caps.h:456,464,465` |
| `EMAC_PTP = 0x50098700` provided by linker | `esp32p4.peripherals.ld:120` |
| `emac_ll_ts_all_enable()` exists but `emac_hal_ptp_start()` never calls it | `emac_ll.h:686`, `emac_hal.c:332+` |
| Board uses internal EMAC + **IP101**, 100 Mbps, PoE | Waveshare wiki ESP32-P4-WIFI6-POE-ETH |

**The one gap:** `ts4all`. Without it the MAC stamps only PTP-matched frames, not NTP-over-UDP.
`esp_eth_mac_enable_ts4all()` is master-only. We set `EMAC_PTP.timestamp_ctrl.en_ts4all = 1`
ourselves after `ptp_start`. One bit; far less invasive than the existing W5500 fork.

---

## Phases

### Phase 1 — Bring-up
- [x] Create `p4probe/` as a standalone ESP-IDF project (not in the ESPHome tree). PlatformIO,
      `framework = espidf`, board `esp32-p4-evboard`; gitignored.
- [x] Configure Ethernet: internal EMAC, IP101, RMII, `CLK_EXT_IN` on GPIO50, pins above.
      `ETH_ESP32_EMAC_DEFAULT_CONFIG()` already matches this board exactly — no pin overrides.
- [x] Flash, confirm link up + DHCP lease. Link at 2.4 s, `192.168.34.172` (same /24 as the S3).
- [x] **Chip revision: v1.3** — boot log and esptool agree.

**Result 2026-09-10: PASSED.** Mac Studio ping 30/30, then 600/600 at 5/s.
- **Acceptance:** the P4 pings, and `ping` from the Mac Studio succeeds.

### Phase 2 — PTP clock alive
- [x] `ioctl(ETH_MAC_ESP_CMD_PTP_ENABLE)` — takes a `bool*`; config is hardcoded internally
      (XTAL, 25 ns period, 40 ns accuracy, FINE, digital rollover). Tick is 40 ns, not selectable.
- [x] Poll `G_PTP_TIME` in a loop; log seconds/nanoseconds.

**Result 2026-09-10: PASSED.** Steady-state steps 999 999 960 / 1 000 000 000 / 1 000 000 040 ns
— the 40 ns tick. These time the report task against the PTP clock, so they prove monotonic
counting and clean rollover, not crystal accuracy (both share the 40 MHz XTAL). Note: a PTP
read returns zeros with ESP_OK while timestamping is off — gate reporting on it being enabled.
- **Acceptance:** the PTP clock advances monotonically at ~1 s/s, no rollover glitches.

### Phase 3 — `ts4all` and RX timestamps (**the risk gate**)
- [x] After `ptp_start`, set `EMAC_PTP.timestamp_ctrl.en_ts4all = 1` (declare `extern emac_ptp_dev_t EMAC_PTP;`).
      Disassembly confirms: `lui/addi → 0x50098700 <EMAC_PTP>`, read, `ori 256` (bit 8 = TSENALL), write.
- [x] Register via `esp_eth_update_input_path_info()`; `info` is `eth_mac_time_t*`.
- [x] Plain non-PTP traffic (DHCP, ARP, ICMP ping from the Mac Studio).

**Result 2026-09-10: PASSED — with a trap found on the way.**
- **`info` is never NULL on P4** (`esp_eth_mac_esp.c:457`, compile-time `&ts`, passed on every
  frame). Counting non-NULL pointers reports 100% regardless. **Zero stamp = not latched.**
- **Control experiment, one boot, ping at 5/s:** ts4all toggled every 10 s, four windows.
  With the bit set, stamps advance and fall inside the preceding PTP second. With it clear,
  every frame still arrives but the stamp is `0.000000000`. Setting it again restores real
  stamps on the very next frame. Reproduced on both cycles. Ping 600/600 throughout.
- Conclusion: `ts4all` is exactly what makes the MAC capture SFD stamps on non-PTP frames.
- **Acceptance:** a hardware RX timestamp arrives for a **non-PTP** UDP frame, and it tracks
  the PTP clock. If this fails, stop and reassess — everything downstream depends on it.

### Phase 4 — TX timestamps
- [x] Transmit via `esp_eth_transmit_ctrl_vargs()` with `ctrl` pointing at an `eth_mac_time_t`.
- [x] Confirm the returned stamp is plausible against the PTP clock.
- **Acceptance:** TX stamps returned for every frame, monotonic, no timeouts.

**Prerequisite found: `CONFIG_ETH_TRANSMIT_MUTEX=y`.** It defaults to `n`, and
`esp_eth_transmit_ctrl_vargs()` only locks when it is set, while the DMA code rewrites the TX
descriptor ring with no lock of its own. lwIP transmits through the same function, so a probe
task sending concurrently could corrupt the ring. With the mutex, the TX-stamp busy-poll runs
inside the lock (lwIP transmits wait for it, sub-ms).

How the driver delivers the stamp (`esp_eth_mac_esp_dma.c:292-304`): with PTP enabled every
TX descriptor requests one, but it is only collected when a `ctrl` pointer is passed, by
busy-polling the last descriptor up to 50 iterations. **Timeout silently zero-fills** — same
sentinel as RX.

**Result 2026-09-10: PASSED.** 90-byte frames (NTP-request size) at 10 Hz to an unowned
locally-administered MAC (the S3's W5500 MAC filter drops it in silicon), EtherType 0x88B5,
with ping at 5/s from the Mac Studio competing for the transmitter:
- 500 frames: **0 timeouts, 0 unbracketed, 0 non-monotonic, 0 send errors.**
- Steady state (min / median / max): read-before → SFD **15.6 / 16.0 / 17.5 µs**;
  SFD → read-after 14.2 / 14.5 / 15.0 µs; call 28 / 29 / 30 µs. Boot window had one 64 µs
  lead during DHCP: contention showing as latency, not corruption.
- Ping 1100/1100, RX still 100% stamped.
- PTP-time bracket (≈30.5 µs) and `esp_timer` call duration (29 µs) agree.

Takeaway: a software T1 taken just before transmit would carry **~16 µs systematic error**
on this board; the hardware T1 removes it. PHY latency (MII SFD → copper) is not visible to a
single device and remains a Phase 7 concern.

### Phase 5 — NTP probe
- [x] Hand-build Ethernet/IP/UDP + 48-byte NTP request (mode 3, VN 4), sent through the ctrl
      path. IPv4 and UDP checksums computed (the ESP EMAC driver never enables checksum insertion).
      **Next hop learned from the reply, not from lwIP's ARP table:** `etharp_find_addr()` would
      race the tcpip thread (`CONFIG_LWIP_TCPIP_CORE_LOCKING` is off). One ordinary NTP request
      goes out through a lwIP socket; the RX hook takes the reply's Ethernet source MAC —
      the server on-link, the gateway if routed, i.e. always the correct next hop.
- [x] Parse T2/T3 from the reply; assemble `(T1,T2,T3,T4)` with hardware T1/T4. Matched by a
      cookie in the transmit field (T1 does not exist until the frame leaves).
      **Probe replies are consumed in the RX hook** (`free(buffer)`): no lwIP socket owns the probe
      port, so forwarding them would make lwIP send ICMP port-unreachable to the device under test.
- [x] UDP command interface on 12300: `PING`, `RUN <ip> <count> <gap_ms>` → `Q` lines + `END`.
      The same priming reply steps the PTP clock once to the NTP era (offsets in µs, not a century).
- **Acceptance:** δ and θ computed on the Mac agree in magnitude with the existing Python
  probe (δ ≈ 130–800 µs depending on path), with visibly lower scatter.

**Result 2026-09-10: PASSED.** Client `p4client.py` (Mac Studio `/root/ntp/`), exact integer ns.
- Run 1: 200/200 ok, run 2: 300/300 ok — 0 timeouts, 0 TX/RX zero stamps, 0 rejects.
  Next hop `98:a3:16:c0:3b:83` = the S3's W5500. T3−T2 2416 µs (Pi measured 2411 µs).
- δ median **114.9 / 114.4 µs**, IQR 21.4 / 19.9 µs (same /24). Reference kernel-stamped Mac
  Studio client, routed: 241.5 µs, offset IQR 18.6 µs.
- **Raw θ is a ramp, not noise.** The free-running PTP clock runs at **+34.106 / +34.180 ppm**
  against the GPS-disciplined server (R² 0.99973 / 0.99990). Run 2 began −8.6 µs from where
  run 1's ramp predicts across a 247 s gap. So the design assumption "c is constant across a
  window" was wrong: c ramps, but linearly and repeatably, so it is removable.
- **Detrended θ:** IQR 10.4 / 9.4 µs, std 8.2 / 7.5 µs — below the kernel-stamped reference, but
  not sub-µs per sample.
- **Attribution:** detrended θ regressed on δ has slope −0.52 / −0.41, corr −0.93 / −0.83.
  That is the signature of T3 stamped early (or return-path queuing): δ +e, θ −e/2. T3 is a
  *prediction* on the S3, so the dominant residual is most likely the device under test,
  which this instrument can now see.

### Phase 6 — Characterise the floor (**go/no-go for Phase 7**)
- [x] Place the P4 on the S3's VLAN so there is no routed hop. **No change needed:** the P4's
      port already sits in the S3's /24 (DHCP `192.168.34.172`).
- [x] Run long captures; report median/IQR of δ and θ, and the short-term stability.
- [x] Compare measured δ against the computed physical minimum for 90-byte frames at 100 Mbit.
- **Acceptance:** a stated, defensible A/B resolution figure. Target: well under 1 µs.
- [ ] Re-run the anchor A/B and the PPS-rate-term change with the new instrument.

**Floor result 2026-09-10** (runs 1+2, 188 + 283 samples after delay-outlier exclusion;
scripts `p4trend.py`, `p4decomp.py`, `p4acf.py` in `/root/ntp/` on the Mac Studio):

| quantity | run 1 | run 2 |
|---|---|---|
| detrended θ std / IQR | 8.18 / 10.44 µs | 7.45 / 9.42 µs |
| of which delay-coupled (T3 signature) | 7.64 µs (slope −0.52) | 6.21 µs (slope −0.41) |
| remainder std / IQR | 2.92 / 3.45 µs | 4.13 / 4.49 µs |
| integrated autocorrelation time | 1.00 | 1.13 |
| **A/B 95% CI, 1000 samples/arm (corrected)** | **±0.72 µs** | **±0.70 µs** |
| A/A null test, max \|diff\|/SE over block sizes 5–50 | 0.61 | 0.92 |

- **Verdict against the target, stated both ways.** Per sample the floor is **not** sub-µs
  (std ~8 µs, most of it the server's T3 prediction). As an **A/B resolution it is**: ±0.7 µs
  at 1000 samples per arm (~8.5 min of exchanges at 4 Hz), with samples effectively
  independent and the A/A null test reporting no false effects. The A/B resolution is what
  the plan's go/no-go needs.
- The A/B figure uses the FULL detrended residual. The delay-coupled split only bounds the
  noise; subtracting delay from an A/B effect would absorb genuine T2/T3 changes.
- **Lead, not a conclusion:** run 1 has ρ(lag 4 = 1 s) = +0.20 and ρ(lag 6) = −0.21; run 2 shows
  +0.10 at lags 4 and 8. A per-second structure in the remainder is where the S3's adjtime
  sawtooth would appear. Confirming needs T2-phase data the current CSVs lack.
- **Physical delay bound (estimate).** 100 Mbit = 80 ns/byte. Outbound: remaining 94 B of the
  frame after the P4's SFD stamp (7.5 µs) + switch egress incl. preamble (8.2 µs). Return:
  full frame into the switch (8.2 µs) + preamble to the P4's SFD (0.6 µs). ≈ 24.5 µs plus
  switch forwarding twice → **~30 µs**. Measured δ min 58.7 µs, median ~114 µs → roughly
  **30–85 µs inside the S3's W5500 receive/transmit path** (T2 lateness + SEND-to-wire).
  First time that sum has been measurable.

**Live A/B method (set up 2026-09-10, before any A/B data existed).**
- A runtime switch for the rate term was added to the S3 — `set_use_hw_rate()` in
  `gps_pps_time`, template switch "NTP HW Rate Term" — so OFF selects the legacy
  position-mean term the fix replaced. The capture EMA keeps updating in both states.
  Flashed by OTA (no GPS power cycle) after checking the deployed `gps_pps_time` equals git
  HEAD, so the flash adds only the setter.
- **10 min settle after the reboot before measuring:** `drift_mean_x256_` restarts from zero
  with a 128 s time constant, and both the legacy term and the adjtime overcorrection use it.
- Driver `p4ab.py`: 20 blocks × 100 exchanges at 250 ms, ABBA arm order, switch set over the
  S3's REST API and verified before each block, 3 s settle, always restored ON.
- Effect from one least-squares fit over all samples, `offset = a + b·t + d·arm` (removes the
  probe's 34 ppm ramp without biasing the arms). 95% CI from the residual SE inflated by
  √τ_int, cross-checked by the spread of ABBA block-quad estimates.

**Pre-registered prediction for the rate-term A/B** (written before the run):
the legacy term is ≈ `drift_mean × 1000` ≈ 4–6 ppm against a true 9.26 ppm, so in the OFF arm
the anchor under-corrects elapsed time by ≈3–5 µs per second. T2 and T3 then run late by that
× the sub-second phase, giving **a sawtooth rising +3 to +5 µs across each second in OFF**,
flat in ON, no delay effect, and a mean ON−OFF offset of roughly **−1.5 to −2.7 µs**.
The sawtooth slope (`p4phase.py`, folding residuals by T2's phase) is the sharper test;
the mean effect only sees half of it.

**Live A/B results 2026-09-10** (S3 flashed 21:48, settled 10 min; `ab-anchor.csv`,
`ab-rate.csv` on the Mac Studio; 2000 exchanges each, 100% answered, both switches restored ON).

*As first analysed — not resolved.* Anchor ON−OFF +2.9 ± 12.8 µs, rate term −9.1 ± 13.2 µs;
residual std 74 / 79 µs (~10× the floor), τ_int 3.7 / 3.4. **The earlier "±0.7 µs at 1000 samples
per arm" claim was only valid for spike-free data** — it came from 50–75 s runs.

*Two wrong explanations, retracted.* (1) Curving probe drift: straight-line residual medians per
block are −13..+4 µs, and Legendre baselines to degree 12 move the residual std by <1 µs. (2) Phase
confounded with time at the 250 ms cadence: phase is uniformly spread within every block
(concentration 0.00–0.06).

*Actual cause — bursts of T3-late errors in the server* (`p4spikes.py`, robust σ from MAD, spike
= |residual| > 5σ): ~1% of exchanges (10 / 14), in bursts of 3–5 consecutive exchanges minutes
apart, 0.2–1.6 ms, with **negative delay** (−1.3 … −2.9 ms, which is why an upper-only delay cut
missed them) and offset ≈ −½·delay — the signature of T3 stamped 1.4–2.9 ms late. A per-quad
split shows it directly: quads without bursts sit at the floor (std 7–14 µs), quads with bursts at
66–132 µs. This is a serving defect clients experience: about 1% of replies are off by up to 1.5 ms.

*Robust results (spikes excluded):*

| A/B | effect ON−OFF | 95% CI | residual std | τ_int | spike rate ON / OFF |
|---|---|---|---|---|---|
| NTP PPS Anchor | −6.19 µs | ±1.09 | 8.38 µs | 2.05 | 0.97% / 0.11% (z +2.56, but 2 clusters) |
| NTP HW Rate Term | **−3.11 µs** | **±0.70** | 7.65 µs | 1.01 | 0.64% / 0.85% (z −0.52) |

Burst-free quads agree without any exclusion: anchor −5.5 / −6.9 / −7.9 µs, rate −3.3 / −2.0 µs.

*Pre-registered rate-term prediction vs result:*
- OFF sawtooth across the second: predicted +3…+5 µs → **+3.41 ± 1.75** ✓
- ON: predicted flat → **−0.83 ± 1.67** ✓
- mean ON−OFF: predicted −1.5…−2.7 µs → **−3.11 ± 0.70** (just beyond, overlapping) ✓
- **Deviation:** the analysis exactly as registered (`p4phase.py`, spikes included) is null (±22 µs).
  Confirmation required the post-hoc 5σ cleaning. The rule is generic and the spike rate is
  arm-independent for this A/B, but it was not pre-registered.

*Conclusion on the rate-term fix:* it removes a within-second sawtooth of the predicted size and
form. Anchor path vs gettimeofday: OFF (gettimeofday) shows its own +5.4 ± 2.0 µs rise across the
second, ON −2.6 ± 1.7 µs; the −6.2 µs mean difference is resolved, but the anchor A/B's spike rate
may depend on the arm, so treat it with more caution than the rate result.

*Corrected A/B resolution:* ±0.7–1.1 µs (95%) at ~930 samples per arm **after removing T3-late
bursts**; ±13 µs without. Fixing the bursts in the server is what makes the resolution real.

**Server defect: root cause (2026-09-10).**

*Timing.* Wall-clocked across three runs (`p4clock.py`): every minute at second 4.6–7.5 (mostly
7.0) a ~1 s receive stall, 50–114 ms per request; every T3-late burst follows one. HA publish
seconds: uptime :02.4, gps_pps_time :03.5, debug :04.8 — none matches.

*Refuted along the way* (each by source or data): flash/NVS access in `update()`; debug heap walk;
missed W5500 interrupt with a ~100 ms poll fallback (interrupt mode re-checks after 1000 ms, and
`poll_timer` exists only in polling mode); main-loop CPU starvation (main task core 0 prio 1,
ntp_recv core 1 prio 7, w5500_tsk prio 15, tcpip prio 18); transmit waiting for W5500 TX space
(it returns ESP_ERR_NO_MEM instead).

*Device-side corroboration* (`/events`): the S3's own `NTP T3 Prediction Error` jumps to 0.4–3.5 ms
and `Hook Latency` to 3.4–7.1 ms right after each stall, and its outbound TCP goes silent ~1 s
then flushes.

*Causal test* (`p4load.py`):
- **A — 5 concurrent web-page downloads (≈390 KB) at second :30 of each minute: a receive stall
  at :30.0–:30.1 in 4 of 4 minutes (140 / 99 / 37 / 94 ms)**, alongside the natural :07 ones.
  Outbound TCP load through the W5500 stalls the receive path on demand.
- B — probe at 1 s, no injected load: the :07 stall persists (the probe does not create it) but
  only 2 of 5 minutes were caught, 7.7 / 14.4 ms. Inconclusive on magnitude: 1 sample/s can
  miss the peak of a ~1 s stall.

*Candidate mechanism (source-level, unproven):* `w5500_send_command()` serves SEND and also the
RECV issued after every received frame; if Sn_CR has not cleared on the first read-back the
caller sleeps 10 ms per retry, up to 100 ms. The 140 ms peak means it cannot be the whole story.

*Self-inflicted load:* the S3 publishes t3_error / int_lead / rx_stamp_gap / hook_latency on every
request (~16 messages/s at a 4 Hz probe) over API and SSE.

*Client impact:* stage 1 — ~1 s per minute of replies with T2 up to ~100 ms late (up to +50 ms
offset; SNTP clients accept them). Stage 2 — stalled SENDs reseed `send_us_`, giving 3–5 replies
with T3 1.4–2.9 ms late and negative delay, which min-delay clock filters prefer.

*Fix options:* harden the T3 reseed (only reseed on mutually consistent rejects); refuse requests
whose arrival-to-handling time exceeds a threshold; throttle per-request telemetry; count Sn_CR
retries to prove the mechanism before touching the vendored driver.

**Fixes implemented 2026-09-10** (all four chosen; one reflash that also restores
`pps_interval_ppb`; verification with `p4verify.py` pending):

- *Refuse stalled requests.* The arrival reference already existed: `eth_input_hook_()` computes
  `int_lead = burst_start - MCPWM INTn edge` (normally 3–25 us) but recorded it only below 20 ms —
  which is why "NTP INT Lead" went silent during every stall. Now `rx_stalled = int_lead >= 1000 us`
  (edges older than 2 s untrusted) is stored in the seqlocked hook ring, and `recv_task_()` drops
  the request and counts it (`refused` sensor). No reply is sent, so a stalled request can no
  longer feed the T3 estimate either.
- *Harden the T3 re-seed.* A re-seed now needs `SEND_RESEED_AFTER` rejects that agree with each
  other within `SEND_STEP_MAX_US` **and** a run spanning at least 3 s (a stall lasts ~1 s). It seeds
  to the run's centre instead of the last sample.
- *Throttle telemetry.* `hook_latency`, `rx_stamp_gap`, `t3_error` and `int_lead` publish every 10 s
  as the window's largest-magnitude value (lock-free accumulators), instead of on every request.
  Their meaning changes from "last request" to "worst in 10 s"; `int_lead` now shows stalls.
- *Instrument the command handshake without vendoring the driver.* The fork's SPI taps see every
  `Sn_CR` write (the command byte) and every `Sn_CR` read-back (its value), so they count completed
  commands, commands needing more than one poll (each a 10 ms sleep), the longest write-to-cleared
  time, and overlapping commands. New sensors `w5500_cmd_retries`, `w5500_cmd_max`.

Built and compiled against the repo test config with no warnings in our code. Before flashing, the
deployed components on the SMB share were checked against git HEAD (`ntp_server`, `ethernet`) and
the working tree (`gps_pps_time`), so the flash ships only these changes.

**Verification 2026-09-10 23:19–23:24** (`p4verify.py` then `p4compare.py`; the same protocol as the
pre-fix `load-A.csv`: 250 ms probe, 5 concurrent web-page downloads at second :30, 4 minutes):

| | before (`load-A.csv`) | after (`verify.csv`) |
|---|---|---|
| replies / requested | 960 / 960 | 945 / 960 (15 refused) |
| replies off > 1 ms, per 1000 | 31.2 | **6.3** |
| replies off > 100 µs, per 1000 | 51.0 | **25.4** |
| worst offset error | 56.1 ms | 38.1 ms |
| robust σ / std without spikes | 7.30 / 8.98 µs | **6.87 / 7.23 µs** |
| 5σ spikes: T2 late (rx stall) | 27 | 9 (all 0.7–2.6 ms) |
| 5σ spikes: T3 early (tx stall) | 11 | 1 |
| 5σ spikes: T3 late (estimator) | 0 | 0 |

- **Refusal works where the stalls were.** The S3's `refused` counter went 0 → 15 and the probe saw
  exactly 15 timeouts, so every missing reply was a refusal, not a loss. They sit at second 7.4–7.7
  every minute and 30.7–31.0 after every injected load; four more in the first minute (cause open).
  The every-minute +18…+48 ms replies are gone.
- **The Sn_CR sleep-loop hypothesis is refuted by direct measurement:** `w5500_cmd_retries` stayed 0
  and the longest handshake was 190–719 µs. Root cause of the stalls remains open.
- **`pps_interval_ppb` is restored** (9187–9250 ppb).
- **Open 1 — transmit stall:** one reply at 21:23:30.2 UTC with 80.3 ms delay and −38.1 ms offset (T3
  early: the packet left ~80 ms after the T3 written into it). Receive-side refusal cannot see it, and
  the S3 records nothing: sends longer than `SEND_US_MAX` are discarded silently.
- **Open 2 — small receive-late replies:** nine 1.4–5.3 ms T2-late replies were not refused, so their
  lateness did not appear as ≥ 1 ms between INTn and burst start. Mechanism unknown.
- **Open 3 — reseed hardening is untested:** neither run triggered a reseed, so this comparison
  cannot show its effect.
- **Correction:** `p4verify.py` reported robust σ 71 µs because it fitted the drift ramp with ordinary
  least squares including the ms spikes, tilting the slope. Refit robustly (`p4compare.py`) the floor
  is 6.87 µs.

**Next investigation (user decisions 2026-09-10/11):** instrument the transmit stall first; explain
the 1–5 ms escapes; provoke a re-seed deliberately; find the stall trigger; and test whether
intensive logging also causes the problem.

*Established before any new experiment — the trigger is wall-clock aligned.* After the 23:08 reboot
every boot-relative S3 publish moved (uptime :02.4 → :35.4, gps_pps_time :03.5 → :32.1, debug
:04.8 → :35.4), yet the stalls stayed at second 7. None of the S3's own 60 s timers (including the
ARP refresh) is the trigger. Remaining candidates: something on the Home Assistant / network side,
or the GPS module's GSV output, whose fix count survives OTA reboots.

*Logging, what is known without measuring.* With `baud_rate: 0` a log line only becomes network
traffic while something subscribes (Home Assistant's log viewer, `esphome logs`, a browser on the
device page). Our components log at INFO; other ESPHome components at the global DEBUG. Measured
next as an A/B with and without an `esphome logs` subscriber.

*Estimator extracted and tested.* `components/ntp_server/send_estimator.h` (dependency-free) replaces
the inline logic with behaviour preserved, including the fallback path.
`tests/send_estimator_test.cpp` — 15 cases, host clang++ already on the machine. A compile failure
is weak evidence, so a mutation test was run: built against the pre-fix re-seed rule, exactly the
three stall tests fail (consistent 1 s burst, inconsistent long stall, 3 s span) and the rest pass.

*Diagnostics (switch "NTP Diagnostics", default off, no traffic while on):*
- per-request ring, 1024 records in PSRAM, joined to the P4's samples by the client transmit field:
  INT lead, stamp gap, hook hit, refusal, unclipped send time, sendto duration, estimate used;
- lwIP core-lock contention probe, taken before the reply is built so a wait cannot leak into the
  served T3 (the lock is non-recursive, so it can never be held into `sendto()`);
- W5500 network activity recorder in the fork's SPI taps: 100 ms × 1200 bins in PSRAM, frames by
  class and direction (API 6053, HTTP 80, NTP, mDNS, ARP, other), SPI-lock wait, Sn_CR retries;
- dumps pulled over UDP 12301 (`CLK` / `REQ` / `NET`) only after a run; Mac side `p4diag.py`.

Experiments on this build: E1 load at :30 (4 min), E2 sustained-load re-seed provocation (2 min),
E3 logging A/B (3 min without, 3 min with `esphome logs`).

**Results 2026-09-11 06:28–06:42 UTC** (flashed 06:16, settled 10 min; `p4diag.py`, then
`p4class.py` and `p4txcorr.py` on the saved dumps, plus Home Assistant sensor history):

| run | replies | refused | robust σ | T2 late | T3 early | T3 late | sends ≥ 5 ms | re-seeds |
|---|---|---|---|---|---|---|---|---|
| E1 web load at :30, 4 min | 942 / 960 | 18 | 6.94 µs | 11 | 2 | 15 | 0 | 1 |
| E2 sustained load 25 s, 2 min | 456 / 480 | 24 | 6.03 µs | 9 | 6 | 0 | 3 | 0 |
| E3a no log subscriber, 3 min | 708 / 720 | 11 (+1 unrecorded) | 5.60 µs | 11 | 5 | 0 | 0 † | 0 |
| E3b `esphome logs` attached, 3 min | 709 / 720 | 11 | 6.92 µs | 10 | 1 | 0 | 0 | 0 |

† one flagged record wrote SEND at 2.0 ms; only `sendto()` returned late (5.3 ms), so no T3 error.

1. **The 1–5 ms receive-late escapes are frames that did not open their own read burst.** All 2,815
   joined exchanges, grouped by what the receive hook knew:

   | hook state | n | delay excess p50 / p95 | off > 100 µs | off > 1 ms |
   |---|---|---|---|---|
   | fresh INTn edge, lead < 200 µs | 2,772 | 0 / 24 µs | 78 | **0** |
   | no usable edge (`int_lead` unknown) | 31 | 2.5 / 4.1 ms | 29 | **18** |
   | edge accepted, lead 200–999 µs | 9 | 1.3 / 9.9 ms | 9 | **3** |
   | send ≥ 5 ms (transmit stall) | 3 | 8.3 / 18.0 ms | 3 | 2 |

   Every reply off by more than 1 ms falls outside the first row. *Mechanism, inferred from the code
   and not yet measured directly:* frames that arrive back to back are read in one driver loop, and
   INTn is a level held low, so the queued frame gets no falling edge of its own. `int_lead` is then
   either unknown (the newest edge is later than the burst start) or taken from a later edge, which
   understates the wait. Their stamp gap of ~400–450 µs, against ~900 µs normally, shows T2 came
   from the size read rather than a fresh burst start.
   *Candidate rule:* refuse when `int_lead` is unknown or ≥ 200 µs. On these data it removes 21 of
   the 23 errors over 1 ms, at the cost of 40 extra refusals (1.4 %). The threshold was chosen after
   seeing the data, so it has to be validated on a fresh run with the prediction written down first.
2. **Transmit stalls come with web-server load.** All three sends of 5 ms or more (5.5, 5.8 and
   17.3 ms) fell inside E2's 25 s of downloads. Only in that window did the core-lock probe wait
   3–80 ms. The SPI lock never waited more than 1.2 ms and Sn_CR never retried. The probable cause
   is contention for lwIP's core lock with the web server's TCP output. This is *not proven*: the
   probe samples the lock just before the reply is built, not inside `sendto()`.
   The moderate slow sends (1.8–4 ms, n = 17) compete with the receive path. 47 % of them share a
   100 ms bin with an SPI-lock wait over 300 µs, against 0.3 % of normal sends, and 53 % share it
   with more than 10 received non-NTP frames, against 12.5 %.
3. **The hardened re-seed held under provocation.** E2's back-to-back downloads produced scattered
   sends (2.8–17.3 ms) that never agreed within 100 µs. There were no re-seeds and no T3-late replies.
4. **New defect: a wrong send estimate can persist for minutes.** The first send after the 06:16 boot
   measured 2,607 µs and seeded the estimate (HA `t3_prediction_error` history). The resync burst's
   sends of ~930 µs were rejected (error −1,675 µs), but they spanned less than 3 s, so no re-seed
   followed. Sparse requests at 06:19–06:26 then *agreed* with the high estimate (errors −33 to
   −73 µs). When the P4's 4 Hz stream (~1,040 µs) started, the estimate took 3.8 s to re-seed
   (06:28:47–51) and served 15 T3-late replies at +0.77 ms. So there appear to be two send-time
   populations, and one EWMA cannot serve both.
   *Idle is not the cause (refuted by `p4idle.py`, 06:45–06:52 UTC).* Pairs of requests from the Mac
   were sent after 0.25–60 s of silence, the second 150 ms after the first, in 24 pairs. Both
   requests of every recorded pair sent in a median 1,038–1,050 µs at every gap (range 989–1,096 µs,
   23 + 23 records), with no dependence on idle time.
   *The slow sends continue, from other clients.* HA shows `t3_prediction_error` of +1,532 µs at
   06:43:37 UTC (diagnostics off) and +1,542 µs at 06:52:18, against an estimate of ~1,030 µs. The
   second fell *inside* the idle run, while every one of the Mac's own sends took ~1.04 ms. So some
   other client's replies go out ~2.56 ms after t0, and the T3 served to it is 1.5 ms early. The
   idle script did not save the ring, so that record is lost.
   *It is the normal state in production.* Overnight, 21:10–06:13 UTC on the fixed firmware with
   no probe running, 83 of 342 `t3_prediction_error` windows were ≥ +1.0 ms, 73 of them between
   +1.50 and +1.75 ms. The error is quantised, not spread out.
   *Whose requests (passive capture `p4quiet.py`, 07:05–07:25 UTC, diagnostics on, no probe
   traffic except 3 P4 requests).* 21 records came in. The 4 slow sends (2,711–2,765 µs) all went
   to clients whose transmit field is all-zero, the SNTP style. None of the 8 other records was
   slow, and 9 other zero-field requests were fast (919–1,075 µs). The Mac's `systemd-timesyncd`
   polls the S3 only every 34 min (offset +13 µs, delay 378 µs) and sent nothing in the window. The
   Pi's chrony does not use the S3. The SMB share shows `zeiss-esp32-s3-eth-poe-01` (another W5500
   ESPHome device) syncing via `sntp` from ntp.gwein.ch. The client IPs of the slow sends remain
   unknown: the Mac cannot see other hosts' traffic, and the ring stores no address.
   *Cause: ARP misses (confirmed, `p4arpjoin.py` on `quiet-net.txt`, 07:08–07:24 UTC).* The
   network bins were joined at each request's real arrival time (t2 + hook latency):
   - All **4 of 4** slow sends coincide with the S3 sending *and* receiving ARP in the arrival bin
     or the next one. The S3 sent ARP in only 0.9 % of bins, so a random 4-bin window has ~4 %.
   - **10 of 10** all-zero-field requests triggered an ARP request from the S3, and **0 of 10**
     requests from other clients did.

   How the two outcomes arise:
   - A reply to an unresolved address is queued (`ARP_QUEUEING` on). `sendto()` transmits only
     the ARP request and returns. The NTP frame leaves when the ARP reply arrives.
   - If the tcpip thread (priority 18) handles that reply before the NTP task (priority 7) reads the
     send stamp, the stamp is the NTP frame: **~2.7 ms, "slow"**.
   - Otherwise the stamp is the ARP request: **919–979 µs**, 60–100 µs under normal because an ARP
     frame is shorter to write. It looks fast, but the reply's real departure is unmeasured and
     later. Those samples lie within ±100 µs of the estimate, so the estimator *learned* them; the
     estimate fell from 1,031 to 1,001 µs during the capture.
   - So **every** ARP-missed reply is served an early T3, by about the ARP round trip (≥ 1.5 ms),
     and the S3's own measurement notices only about half of them.
   - lwIP here uses `ARP_TABLE_SIZE` 10 and `ARP_MAXAGE` 300 s. `arp_primes` rose about once a
     minute overnight.
   - Why this one client misses every time, despite the 60 s ARP refresh, is not yet explained.
     Candidates: eviction from the 10-entry table, or rotation out of the 8 tracked-client slots.
   - The 2,607 µs boot seed fits the same mechanism: the first reply after boot has no ARP entry.

   *Confirmation run registered before its data was read:* `p4arplocal.py`, from a second host,
   up to 30 min from 07:30 UTC. Prediction: every send ≥ 1.8 ms coincides with S3 ARP TX and RX
   within −0.1…+0.3 s, and no request from a client with a non-zero transmit field shows S3 ARP
   TX there.
   **Result (07:30–07:56 UTC; 17 records, 5,546 bins with traffic; the S3 sent ARP in 1.0 % of
   bins): both parts pass.**
   - Slow sends (2,712–2,741 µs) with S3 ARP TX and RX adjacent: 4 of 4, where chance is ~4 %.
   - All 11 requests with S3 ARP TX nearby carried an all-zero transmit field. No other request
     had one.
   - 7 of those 11 recorded a normal-looking send of 958–1,069 µs (the ARP frame's stamp).
   - 8 of the 11 matched a hook entry older than 1 s (13–953 s).

**Fixes approved 2026-09-11** (one reflash; implemented by the firmware agent; host-tested):
- **A. Resolve ARP before stamping T3.** Before the reply is built, look up the next hop: the
  client if it is on-subnet, otherwise the gateway.
  - On a miss, send one `etharp_query()`, then poll `etharp_find_addr()` in 1 ms steps, without
    holding the core lock while sleeping.
  - Continue once resolved; refuse if still unresolved after 20 ms.
  - The SEND stamp carries the frame class, and the estimator learns only from NTP frames.
  - New sensor `arp_waits`.
- **B. Receive-time matching.** The key becomes transmit field + source IP + source port. Lookup
  takes the newest match under 1 s old. A request with no match is refused (only while the hook is
  installed).
- **C. Strict RX admission, behind A/B switch "NTP Strict RX Admission" (off until verified).**
  Refuse when there is no usable INTn edge, or the edge lead is 200 µs or more; legacy rule: 1,000 µs.
- **D. Recorder.** An IPv6 frame class, plus a 32-entry multicast/broadcast talker table (source
  MAC, EtherType, first destination MAC, frames, frames inside busy bins), dumped with `TLK`.
- REQ records gain `arp_wait_us`, `refuse_reason` (1 old edge, 2 no edge, 3 hook miss,
  4 ARP unresolved) and `send_class`; new flags 0x40 ARP miss, 0x80 SEND not the NTP frame.

*Implementation notes (reviewed 2026-09-11 before flashing).*
- **New headers:** `arp_wait.h`, `hook_ring.h`, `rx_admission.h` (in `ntp_server`) and
  `w5500_talker_table.h` (a second, self-contained fork file in `ethernet`).
- **Host tests:** five suites pass with `-Werror`. A mutation check (first match, no age check)
  fails exactly the newest-wins and age-limit tests of `hook_ring`.
- **Deviation, accepted:** an off-subnet client with no configured gateway skips the ARP step
  rather than being refused.
- **Review fix:** the receive hook matched a hard-coded destination port 123 while the socket
  binds the configurable `port`. With refuse-on-miss, any other port would have refused every
  request. It now compares against the bound port.
- **Other changes:** the fallback learn from the `sendto()` duration now runs only when no SEND
  happened at all. Two pre-existing `-Wformat` warnings in the touched files were fixed.
- **Dump formats:** REQ has 14 tokens; NET has 24 tokens (9 classes: api, http, ntp, mdns, arp,
  tcp, udp, ipv6, other); `TLK` is new. Verification scripts: `p4verify2.py` (predictions 1, 2
  and 4) and `p4strict.py` (prediction 3).

**Pre-registered predictions for the verification after that reflash** (written before any data):
1. **ARP (30 min passive capture, diagnostics on, strict switch off).**
   - No send ≥ 1.8 ms outside web load.
   - Requests with `arp_wait_us > 0` have a `send_class` of NTP and a send within ±150 µs of the
     estimate.
   - Over the following night, windows with `t3_prediction_error` ≥ +1.0 ms fall from 83/342
     (24 %) to ≤ 2 %.
2. **Matching.** Every all-zero-field request in the capture has a hook latency under 1 s, and
   Home Assistant's `hook_latency` maximum stays under 1 s for 24 h.
3. **Strict admission (P4, 250 ms probe, web load at :30).** Interleaved ABBA blocks of 4 min
   each, switch off / on / on / off, repeated twice (16 min per arm). With the switch on:
   - replies off by more than 1 ms: ≤ 2 per 1,000 (off arm expected ~8 per 1,000);
   - refused requests: at most 2.5 percentage points more than the off arm;
   - robust σ: within ±1 µs of the off arm.
4. **:07 burst.** Success means identification, not a numeric effect. NET and TLK dumps covering
   at least 3 minutes give the burst's IPv6 share, and the source MACs covering ≥ 80 % of
   burst-bin frames.

**Verification, part 1 — passive capture `p4verify2.py`** (08:31–09:01 UTC; flashed 08:19:59,
settled 10 min; strict switch off; 24 request records, 4,572 bins with traffic):

| prediction | result |
|---|---|
| 1 ARP, capture part | **PASS.** No sends ≥ 1.8 ms. 6 of 6 replies served after an ARP wait (872–1,470 µs) had an NTP-class SEND within ±150 µs of the estimate. No SEND was stamped from a non-NTP frame. |
| 2 matching, capture part | **PASS.** 13 of 13 zero-field requests matched a fresh entry (hook latency median 1,651 µs, max 1,774 µs). No hook-miss refusals. |
| 4 :07 burst | **PASS (identified).** Busy bins were 380 of 4,572. Their received frames: 80 % "other", 19 % mDNS, **1 % IPv6**. One source MAC, `6c:63:f8:7e:15:e3`, sent 59,787 frames with **EtherType 0x8100 (802.1Q VLAN-tagged)** to `ff:ff:ff:ff:ff:ff`; 23,334 of them fell in busy bins, 79.9 % of all busy-bin multicast/broadcast frames. Its untagged mDNS (7,506 frames) brings the total to 91.2 %. |

**Not predicted — ARP-unresolved refusals.** 6 of 24 requests, all zero-field, were refused
after the 20 ms wait.
- Every one was followed by the same kind of request 3.0–4.0 s later, served with `arp_wait 0`
  and a normal send (1,011–1,064 µs). The client retried and got a correct reply.
- Where visible, the ARP reply arrived in the next 100 ms bin. So this client answers ARP in
  roughly 20–100 ms, consistent with a power-saving Wi-Fi device.
- Cost: one retry per miss. No wrong timestamp was served.
- Not decided: raising `ARP_WAIT_MAX_US` to ~250 ms would avoid the retry at no accuracy cost.
  Replies queued behind the wait would go out later, but their T2 is already hook-stamped and
  their T3 is taken after the wait.
- In 2 of the 6 refusals the ARP frames were not visible in the bins. That is unexplained, and
  probably the recorder's bin-roll race.

**The :07 burst is a network configuration matter, not firmware.** VLAN-tagged broadcasts
arriving at the S3 mean its switch port forwards tagged VLANs besides the native one. The W5500
cannot filter by EtherType, and blocking broadcast would break ARP. The remedy is on the switch:
native VLAN only, tagged VLANs blocked. The OUI `6C:63:F8` is registered to Ubiquiti Inc
(maclookup.app), so the sender is a UniFi device. Which one, and whether blocking tagged VLANs on
that port is acceptable, is the user's to confirm in UniFi.

**Verification, part 2 — strict RX admission A/B `p4strict.py`** (09:04–09:40 UTC, from the Mac;
P4 at 250 ms with 5 concurrent web downloads at :30 each minute; blocks of 4 min in the order
off/on/on/off twice):

| block | 0 off | 1 on | 2 on | 3 off | 4 off | 5 on | 6 on | 7 off |
|---|---|---|---|---|---|---|---|---|
| replies / 960 | 946 | 933 | 936 | 946 | 952 | 933 | 937 | 946 |
| refused (S3 counter, all clients) | 17 | 28 | 29 | 14 | 7 | 26 | 24 | 16 |
| replies off > 1 ms | 5 | 1 | 0 | 9 | 9 | 1 | 0 | 3 |
| robust σ, µs | 7.80 | 7.60 | 8.19 | 6.65 | 7.23 | 7.85 | 7.29 | 7.04 |

| pre-registered prediction | off arm | on arm | result |
|---|---|---|---|
| replies off > 1 ms ≤ 2 per 1,000 with the rule on | 6.86 per 1,000 (26 of 3,790) | **0.53 per 1,000 (2 of 3,739)** | **PASS** |
| refused share at most +2.5 points | 1.41 % | 2.79 % (+1.38 points) | **PASS** |
| robust σ within ±1 µs | 7.13 µs | 7.73 µs (+0.60) | **PASS** |

- **Errors over 1 ms fell 13-fold.** 26 events vs 2 in near-equal reply counts. Under equal rates
  a split that lopsided has probability 3.6 × 10⁻⁶, about 1 in 280,000 (two-sided binomial: the 28
  events split in proportion to the arms' reply counts).
- **Decision (user, 2026-09-11): strict admission on by default.** The switch was turned on at
  once via REST. The C++ default is now `true` and the switch `restore_mode` is `ALWAYS_ON`; the
  reflash is held so it can ship with the pending ARP-wait decision. **Flashed 10:29:45 UTC** (no
  other change, since the ARP wait stayed 20 ms). After the reboot the switch came up ON by itself,
  NTP answered within 25 s, and the share matched the flashed tree. The 10 min settle had 1 ARP
  wait and 1 refusal.
- **Decisions (user, 2026-09-11):**
  - **ARP wait stays 20 ms.** Raising it would trade one retry for blocking every other client. The
    UDP queue holds only 6 requests, and an unanswered ARP from a forged or absent on-subnet source
    would cost a full wait per packet.
  - **The tagged broadcasts are removed at the switch.** The user restricts the S3's port to its
    native VLAN. The passive capture is then repeated to confirm; prediction: no busy bins
    dominated by 0x8100 frames, and tagged background ≈ 0/s (it was 8.8/s).
  - **Result — PASS** (user set the S3's UniFi port profile to native "smart (34)" with Tagged
    VLAN Management "Block All"; 10 min capture from 10:18 UTC, strict admission on):

    | | before (32.4 min) | after (10.1 min) |
    |---|---|---|
    | non-IPv4/ARP/IPv6 ("other", i.e. 802.1Q-tagged) frames | 20.5 /s | 0.01 /s (4 frames) |
    | busy bins (> 30 received frames per 100 ms) | 380 (11.7 /min), 80 % tagged | 38 (3.8 /min), 95 % mDNS, 5 % IPv6, no tagged |
    | largest bin | up to ~150 frames | 45 frames |
    | 0x8100 talkers in TLK | `6c:63:f8:7e:15:e3` | none |

    `6c:63:f8:7e:15:e3` now reaches the S3 only with untagged mDNS (IPv4 and IPv6) and ARP
    broadcasts, consistent with it being the subnet's UniFi gateway. Predictions 1 and 2 (capture
    parts) passed again on this smaller sample: 2 ARP-waited replies on time; zero-field hook
    latency at most 1,749 µs. One more ARP-unresolved refusal occurred.
- **The σ difference is inside the tolerance but not zero.** The on block exceeded its
  neighbouring off block in three of the four pairs. Small effects of this size need more blocks
  to resolve. A plausible mechanism, not tested: refusing edge-less requests removes the fastest
  back-to-back replies as well as the late ones.
- The switch is left off. Turning it on by default needs code (default and `restore_mode`), and
  that is the user's decision.
- Still open for prediction 1 and 2: the overnight `t3_prediction_error` share (≤ 2 % of windows
  ≥ +1.0 ms) and 24 h of `hook_latency` < 1 s, both from Home Assistant history.

### Server tails — what the per-sample floor is made of (analysis, 2026-09-11)

*Data:* E1–E3 (this morning's diagnostics runs), joined per exchange to the S3's own record.
*Method:* `p4tail3.py`, restricted to exchanges with a fresh INTn edge (0 < lead < 200 µs) and a
normal send.
*Join caveat:* the request ring outlives a run while the P4's cookie restarts at 1, so a dump can
contain an older record with the same cookie. Keeping the last occurrence (the ring is dumped
oldest first) dropped 62, 303 and 300 stale duplicates from E2, E3a and E3b. `p4diag.py` and
`p4class.py` already kept the last one, so their earlier results are unaffected. First-pass
`p4tail.py`/`p4tail2.py` numbers were contaminated and are superseded.

| per-sample offset residual (2,772 exchanges) | robust σ |
|---|---|
| as measured | 6.58 µs |
| after removing the server's T3 prediction error (+t3err/2) | **1.95 µs** |
| after also removing the INTn lead (−lead/2) | **1.05 µs** |

- **The physics checks out by robust regression** (2,682 of 2,772 exchanges retained):
  offset = −2.91 **−0.515**·t3err **+0.480**·lead, residual robust σ 1.01 µs;
  delay = −4.3 **+1.032**·t3err. The expected coefficients are −0.5, +0.5 and +1.0.
- **Conclusion:** the server's send-time prediction jitter (σ 11.9 µs) is the per-sample floor.
  Probe, wire, P4 PHY and INTn lead together leave ~1 µs.
- **Tail > 100 µs: 78 exchanges.**
  - 50 are T3 prediction error (within 50 µs once it is removed).
  - 22 are *T2 late despite a fresh edge*: delay excess 1.1–7.6 ms with a lead of 3–18 µs.
    Hypothesis: a lost-interrupt race. The frame arrives while the driver drains an earlier one,
    the interrupt is cleared without it, and the next frame's edge triggers the read.
  - 4 form one whole-second offset jump (06:36:47 UTC, −639…−653 µs, delay normal); cause unknown.
  - 2 other, of which 1 fits "reply delayed after SEND".
- **Retraction:** an intermediate, contaminated pass suggested ~15 replies delayed on the way to
  the P4 (egress queueing). After de-duplication only 1 remains. That hypothesis is dropped.

*Proposed next (awaiting approval):* a diagnostics-only build.
- For each NTP reply, stamp the W5500 TX-buffer write (start and end). This splits the T3 error
  into the stack part and the write→SEND part, and locates web-load stalls.
- Record `Sn_RX_RSR` at our frame's read, next to its length, to test the lost-interrupt race.

The outcome decides whether a *late T3 patch* in the TX tap (rewrite T3 plus the UDP checksum at
the buffer write) can lower the per-sample floor for all clients.

**Approved by the user 2026-09-11. Pre-registered predictions for diagnostics build 2** — written
and committed before the build exists or any of its data is seen.

*Protocol.* Flash, then settle 10 min, with strict RX admission ON (the new default). Diagnostics
are switched on per run.
- P4 at 250 ms: six back-to-back 4-minute `p4diag.py load` runs (5 concurrent web downloads at
  :30 each minute).
- Then one 3-minute `p4diag.py reseed` run (25 s of sustained downloads from :40).
- REQ is dumped after each run; joins keep the last cookie occurrence.

*Quantities,* per served P4 reply whose SEND is the NTP frame:
- A = TX-buffer write start − t0
- B = SEND − write start (so send = A + B)
- t3err = send − estimate

*Predictions:*
- **P1 — where the send-time jitter lives.** Robust σ(B) ≤ 4 µs and robust σ(A) ≥ 8 µs.
  *Decision rule:* a late T3 patch at the buffer write is designed only if σ(B) ≤ 4 µs.
- **P2 — where transmit stalls wait.** Every send ≥ 5 ms has A ≥ 0.9 × send: the wait comes
  before the buffer write, consistent with core-lock contention.
  If no send ≥ 5 ms occurs, P2 is reported as untested.
- **P3 — lost-interrupt race.** Among served exchanges with a fresh edge (0 < lead < 200 µs) and
  a late T2 (delay excess − t3err > 1,000 µs, offset consistent with a late T2), the receive-size
  register at our read shows ≥ 60 bytes queued behind our frame (RSR − (frame length + 2)) in
  ≥ 80 % of cases. Among exchanges with |delay excess − t3err| < 100 µs it does so in ≤ 5 %.
  - With fewer than 8 late cases, P3 is reported as insufficient data.
  - *Decision rule:* an RSR-based refusal is designed only if P3 passes.
  - *Sanity check before judging P3:* in normal exchanges the most common queued-behind value
    must be 0. Otherwise the length bookkeeping (the 2-byte MACRAW header) is wrong, and P3 is
    re-evaluated after correcting it, with the correction reported.
- **Also reported, no prediction:** any whole-second offset jump, meaning ≥ 3 consecutive
  exchanges within one second with |offset| > 300 µs and normal delay.

**Results — diagnostics build 2** (flashed 11:25:01 UTC and settled; strict admission on;
protocol 11:35–12:05 UTC):
- **Runs:** six 4-min load runs with 944–949/960 replies each; one 3-min sustained-download run
  with 692/720.
- **Data:** 6,720 P4 request records, 6,373 joined to a probe sample.

| prediction | measured | verdict |
|---|---|---|
| **P1** σ(B) ≤ 4 µs and σ(A) ≥ 8 µs | n = 6,610: A (t0 → write start) median 831 µs, robust σ **11.9 µs**; B (write start → SEND) median 254 µs, robust σ **5.9 µs**; write duration median 198 µs, robust σ 5.9 µs; t3err robust σ 11.9 µs | **FAIL** (σ(B) > 4). Per the rule, a late T3 patch *at the buffer write* is not pursued. |
| **P2** every send ≥ 5 ms has A ≥ 0.9 × send | one stall: send 11,698 µs, A 11,425 µs (98 %) | **PASS**, n = 1 (weak) |
| **P3** ≥ 80 % of late-T2 cases queued-behind, ≤ 5 % of normal | sanity check passed (the queued-behind mode in normal exchanges is 0, in 6,302 of 6,313). Late n = 28, flagged **21 %**; normal flagged 0.2 % | **FAIL.** The lost-interrupt race accounts for about one fifth of late receives. |
| whole-second jumps | 0 found | — |

What the failures show (observations, not yet tested):
- **P1.** The 5.9 µs left after the write start is the SPI transfer of the frame itself.
  "Write end → SEND" was not part of the prediction; its jitter decides whether patching T3
  *after* the buffer write, just before SEND, is worth a separate pre-registered test.
- **P3.** Most late cases share a signature: the interrupt lead is ~24 µs (normal median 9 µs),
  nothing is queued behind, and the lateness clusters at 1.39–1.65 ms (one case 2.9 ms). The
  driver reacted promptly to an INTn edge that came about 1.5 ms after the frame reached the
  probe's view of the path. Candidates:
  - the W5500 interrupt low-level timer (INTLEVEL) delaying a re-asserted interrupt;
  - queueing on the switch path toward the S3, which from the probe looks identical.

*Follow-up facts (computed and looked up after the verdicts, so leads rather than results):*
- **Write end → SEND is tight:** n = 6,610, median 56 µs, robust σ **3.0 µs** (p1 42, p99 76).
  - Correlations between the parts are small: A–W −0.12, W–E +0.28, A–E −0.07.
  - A T3 patched *after* the TX-buffer write, just before SEND, would therefore leave about 3 µs
    of jitter plus the patch write's own, against 11.9 µs today.
  - That variant was not covered by P1's rule and would need its own pre-registered test.
- **INTLEVEL is set to its maximum.** ESP-IDF v5.5.1 `esp_eth_mac_w5500.c` (public upstream
  source) writes `INTLEVEL = 0xFFFF`, with Sn_IMR(0) = RECV only and SIMR set by the driver.
  - The W5500 datasheet defines Interrupt Assert Wait Time
    IAWT = (INTLEVEL + 1) × 4 / PLL_CLK, with PLL_CLK = 150 MHz.
  - That gives **IAWT = 65,536 × 4 / 150 MHz = 1.748 ms**. "When the next interrupt occurs, INTn
    asserts after INTLEVEL time."
  - This matches the 1.39–1.65 ms cluster. Hypothesis: a request arriving within ~1.75 ms of the
    chip's previous interrupt has its INTn, and so T2, delayed by the remainder of that wait.
    Delays below 1 ms would then populate part of the 0.1–1 ms T2 tail.
  - Not yet tested by an intervention.
  - **Supporting shape (post hoc).** Lateness histogram of the 55 T2-late-consistent exchanges
    (> 100 µs) among 6,371 fresh-edge served exchanges, per 250 µs bin from 100 µs:
    3, 8, 6, 11, 10, 8, 8. Then **0 between 1.75 and 2.0 ms**, and 1 case at 2–3 ms.
    - The spread is roughly uniform up to a sharp cutoff at IAWT = 1.748 ms, as expected if the
      interrupt is held until the wait ends.
    - The median lead is 19–24 µs in every bin (normal: 9 µs).
    - Only 7 of 55 had a frame queued behind.
  - **Test that would decide it:** lower INTLEVEL at runtime, behind a dead-man revert, in an
    interleaved A/B.

*Facts for the two follow-up designs* (public sources, 2026-09-11):
- **ESP-IDF v5.5.1 `emac_w5500_transmit`, in order:**
  1. Read Sn_TX_FSR twice, until the reads agree.
  2. Read Sn_TX_WR.
  3. Write the frame into the TX buffer: one write, or two if it wraps the ring.
  4. Write Sn_TX_WR = offset + length.
  5. Write Sn_CR = SEND, then poll Sn_CR (10 ms sleep per retry).
  6. Poll Sn_IR until SEND_OK, then clear it.

  So exactly one 2-byte register write (Sn_TX_WR) separates the end of the buffer write from
  SEND; that gap is the measured median 56 µs, robust σ 3.0 µs. The frame is not transmitted
  until SEND, so bytes in the TX buffer can still be rewritten at the SEND tap.
- **RX task:** read Sn_IR; if RECV, clear it first, then drain frames while `packets_remain`. When
  no notification arrives for 1 s, it checks the INTn level (added to recover from a missed edge).
- **INTLEVEL:**
  - The reset value is **0x0000**.
  - ESP-IDF commit d09f6ca raised it to 0xFFFF so that a re-asserted interrupt is not missed by the
    edge-triggered GPIO. A missed one left the driver silent; a later commit (0cd021a) added the
    1 s recovery.
  - A value of 0 therefore risks receive stalls of up to 1 s. A short non-zero wait (0x0FFF →
    IAWT 109 µs) keeps a guaranteed high period before re-assertion.

**Designs A and B approved by the user 2026-09-11.** One firmware carries both, each behind its
own switch, off by default. Predictions are written and committed before the build exists.

*Design A — INTLEVEL 0x0FFF behind "W5500 Short Interrupt Wait" (30 min dead-man revert).*
Protocol:
- after the flash and a 10 min settle, with strict admission on, diagnostics on and post-write T3
  off;
- P4 at 250 ms with 5 web downloads at :30 each minute;
- eight 4-min blocks in switch order off/on/on/off, repeated twice; each on-block re-arms the
  dead-man;
- joins keep the last cookie occurrence.

Predictions:
- **A1.** Among served fresh-edge exchanges, the share that is T2-late-consistent with lateness
  > 150 µs is ≤ 0.1 % in the on arm (off arm expected ≈ 0.8 %, as in build 2).
- **A2.** On arm, excluding exchanges with a frame queued behind, the largest T2-late-consistent
  lateness is ≤ 250 µs (IAWT 109 µs plus classification noise).
- **A3.** P4 missing replies, on arm minus off arm: ≤ 0.5 percentage points.
- **A4.** No receive stall ≥ 100 ms in the on arm: no refusal with int_lead ≥ 100 ms, and no
  probe delay excess ≥ 100 ms.
- **Validity check.** INTLEVEL reads back 0x0FFF in on-blocks and 0xFFFF in off-blocks; otherwise
  the run is invalid.
- *Decision:* if A1–A4 pass, propose 0x0FFF as the default (a separate decision).

*Design B — T3 rewritten at the SEND tap behind "NTP Post-Write T3".*
Protocol:
- INTLEVEL at A's verified setting (0xFFFF if A failed);
- eight 4-min ABBA blocks on this switch; P4 at 250 ms with load at :30; diagnostics on;
- for the whole run the Mac sends one kernel-socket NTP request per second to the S3, recording
  replies and `/proc/net/snmp` Udp InCsumErrors before and after.

Predictions:
- **B1.** On-arm per-sample robust σ of the P4 offset residual (median over blocks) ≤ 3.5 µs; the
  off arm is expected at ≈ 6.6–7.7 µs.
- **B2.** On arm, the robust σ of the patch→SEND prediction error per reply is ≤ 5 µs.
- **B3.** The Mac's InCsumErrors is unchanged, and no Mac request goes unanswered except those the
  S3 counts as refused.
- **B4.** P4 missing replies, on minus off: ≤ 0.5 percentage points.
- **B5.** |on − off mean offset shift| (median of block means) ≤ 10 µs.
- *Decision:* if B1–B5 pass, propose post-write T3 as the default (a separate decision).

**Results — design A** (build flashed 12:51 UTC, settled; A/B 13:02–13:38 UTC from the Mac,
`p4intl.py`). All 8 blocks were valid: INTLEVEL read back 0x0FFF with active = 1 in every
on-block, and 0xFFFF with active = 0 in every off-block.

| prediction | off arm | on arm | verdict |
|---|---|---|---|
| **A1** late T2 > 150 µs share ≤ 0.1 % | 0.87 % (33 of 3,774) | **0.05 %** (2 of 3,731) | **PASS** (17-fold drop) |
| **A2** max remaining late (not queued) ≤ 250 µs | 1,594 µs | 2,376 µs | **FAIL** |
| **A3** missing replies on − off ≤ 0.5 points | 66 / 3,840 (1.72 %) | 108 / 3,840 (2.81 %) | **FAIL** (+1.09 points) |
| **A4** no receive stall ≥ 100 ms | 0 | 0 | **PASS** |

- **The INTLEVEL mechanism is confirmed:** the 1.75 ms interrupt assert wait caused almost all
  late receives that still had a fresh edge.
- **Per the pre-registered rule, 0x0FFF is not proposed as the default** (A2 and A3 failed).
- **Design B therefore runs with INTLEVEL at the driver default 0xFFFF**, as the protocol states.

*What A3's extra missing replies are* (post hoc, from the S3's records of every missing P4
request):

| missing P4 replies | off arm | on arm |
|---|---|---|
| refused, old edge (lead ≥ 200 µs) | 62 | 74 |
| refused, **no usable edge** | 4 | **34** |
| served but no reply / no S3 record (lost) | 0 | 0 |

- **No losses in either arm.** The +42 missing replies are refusals, mostly the strict rule's
  no-edge test (4 → 34).
- **Most on-arm no-edge refusals had nothing queued behind** (RSR = frame length + 2).
- *Hypothesis, not tested:* with a 109 µs wait, INTn re-asserts while the driver is still
  draining. The captured edge then falls after the read burst it should explain (lead ≤ 0), and
  the strict rule refuses a request that was probably not late. Short wait and strict admission
  interact; combining them would need an edge-association change plus its own pre-registered
  test.
- The two remaining late cases in the on arm (A2) are rare (2 of 3,731) and bounded by nothing
  measured here.

**Results — design B** (same build; A/B 13:39–14:14 UTC from the Mac with `p4patch.py`, INTLEVEL at
the driver default per protocol; eight 4-min blocks off/on/on/off ×2; the Mac's 1 Hz
kernel-socket sender ran throughout).

| block | 0 off | 1 on | 2 on | 3 off | 4 off | 5 on | 6 on | 7 off |
|---|---|---|---|---|---|---|---|---|
| replies / 960 | 944 | 950 | 949 | 946 | 942 | 945 | 948 | 946 |
| robust σ (joint fit), µs | 8.40 | 3.11 | 2.88 | 6.65 | 7.17 | 2.78 | 2.74 | 7.11 |

| prediction | measured | verdict |
|---|---|---|
| **B1** on-arm σ (median of blocks) ≤ 3.5 µs | off 7.14 µs → on **2.83 µs** | **PASS** |
| **B2** patch→SEND prediction error σ ≤ 5 µs | n = 3,202, robust σ **4.45 µs**, median +2.0 µs | **PASS** |
| **B3** InCsumErrors unchanged; unanswered ≤ refused | InCsumErrors 0 → 0; Mac sent 1,996, replied 1,913 (83 unanswered); S3 refused +206 | **PASS** |
| **B4** missing on − off ≤ 0.5 points | on 48 / 3,840, off 62 / 3,840 (−0.36 points) | **PASS** |
| **B5** \|on − off offset shift\| ≤ 10 µs | joint robust fit: **+11.71 µs** (P4 ramp +33.74 ppm) | **FAIL** |

- **The patch cuts the per-sample floor 2.5-fold and is safe on the wire.** No checksum errors;
  matched queries at second :45 were answered 10/10 in both an off-block and an on-block.
- **Per the pre-registered rule, post-write T3 is not proposed as the default** (B5 failed).
- **The shift has an unknown sign of correctness.** The patch moves served T3 later by ≈ 23 µs on
  average (offset +11.7 µs). The probe has no absolute reference, so it cannot say which T3 is
  closer to true time; that is Phase 7's question.
- *Candidate explanation, to be checked in the code:* the estimate path computes T3 inside
  `build_ntp_response_`, before `t0`, the point from which its send time is learned. That would
  leave a systematic early bias the patch removes.
- *Code check.* Both paths use the PPS anchor. The estimate path's T3 is
  `get_ntp_timestamp_(estimate)` at the end of `build_ntp_response_`. After it come
  `w5500_send_stamp()` and then `t0`. So the old T3 is early by the gap between its clock read and
  t0. Explaining the shift needs a gap of ≈ 21 µs; not yet measured.

**C — what moved the mean offset (user decision 2026-09-11: measure first). Pre-registered
before the build.**
- **Diagnostic:** per reply, `t3_calc_to_t0_us` = t0 − the esp_timer reading
  `get_ntp_timestamp_()` used for the estimate-path T3. REQ gains one field.
- **Protocol:** short wait off; strict admission on; diagnostics on; P4 at 250 ms with load at
  :30. Four 4-min blocks on "NTP Post-Write T3", in the order off, on, on, off.
- **Model, derived before data:**
  - the estimate-path T3 names SEND − gap − t3err;
  - the patched T3 names SEND − patch_err;
  - so the on − off offset shift = (gap + t3err − patch_err) / 2, using medians (t3err from the
    off arm, patch_err from the on arm).
- **C1.** The median gap in the off arm is between 15 and 27 µs.
- **C2.** The model's shift is within ±3 µs of B's measured +11.71 µs, and within ±3 µs of this
  run's own on − off shift (joint robust fit).
- *Decision:* if C1 and C2 pass, the shift is the old path's bias being removed, and post-write T3
  is proposed as the default. Otherwise it stays off and the discrepancy is reported.

*Non-P4 requests during the B run* (`p4nonp4.py`, from the S3's REQ dumps; the ring keeps each
block's last ~4 min). Random-transmit clients, i.e. the Mac's sender plus e.g. timesyncd:
- **Off arm:** served 807 (94.3 %), refused old edge 32, no edge 3.
- **On arm:** served 807 (94.9 %), refused old edge 27, no edge 9.
- **Patched:** 0 of 807 served in the off arm, **807 of 807** in the on arm. Routed clients got
  patched replies too, with no checksum errors.
- **Zero-transmit clients:** ARP-unresolved refusals 6 (off) vs 2 (on).

**A′ — short interrupt wait with edge-aware admission (approved by the user 2026-09-11, one build
together with C).**
- **With the short wait active** (INTLEVEL read back 0x0FFF), strict admission changes:
  - OLD_EDGE (lead ≥ 200 µs) is still refused;
  - NO_EDGE is refused only when a frame was queued behind ours (RSR − (frame_len + 2) ≥ 60 bytes);
  - otherwise the request is accepted, because the chip's wait bounds that lateness at 109 µs.
- **With the short wait off,** the current strict rule is unchanged, so the dead-man revert also
  reverts the rule.
- **Protocol:** eight 4-min blocks, off/on/on/off ×2, on "NTP W5500 Short Interrupt Wait";
  post-write T3 off; diagnostics on; P4 at 250 ms with load at :30; INTLEVEL read-back validity
  checked per block.
- **Predictions:**
  - **A′1.** On arm: served fresh-edge exchanges with late T2 > 150 µs ≤ 0.1 %.
  - **A′2.** Missing P4 replies, on − off: ≤ 0.5 percentage points.
  - **A′3.** On arm: ALL served exchanges (fresh edge or accepted without one) with late T2 >
    150 µs ≤ 0.15 %.
  - **A′4.** No receive stall ≥ 100 ms in the on arm.
- *Decision:* if all pass, propose short wait plus edge-aware admission as the default. The earlier IPv6-block idea (Sn_MR MIP6B) is dropped: IPv6 is only 1 %.
   *Stale hook matches are the rule for zero-field clients, not the exception:* 9 of the 13
   zero-field records in this window matched an entry 15–382 s old.
   *SNTP clients* use T3 alone, so they receive a clock set ~1.5 ms early; their T2 is not used.
5. **The :07 trigger is a one-second multicast burst on the LAN.** At :06.8–:07.8 of every minute,
   in every run, the S3 receives ~100 frames per 100 ms: ~17–24 mDNS and ~80 that are not IPv4. It
   answers with mDNS, SPI-lock waits reach 0.2–0.3 ms, and receive stalls of 20–320 ms follow.
   The source is not identified, because the recorder keeps no MAC addresses. A phase that survives
   S3 reboots fits any external 60 s timer. Relevant facts:
   - The build has `CONFIG_LWIP_IPV6` off, so every IPv6 frame is read over SPI only to be dropped.
   - The IDF 5.5.1 driver's multicast block (Sn_MR bit 5, MMB) covers IPv4 only; the driver itself
     logs "W5500 always filters in IPv6 multicast frames".
   - The W5500 datasheet's Sn_MR bit 4 (MIP6B) blocks IPv6 in MACRAW mode.

   *If* those ~80 frames per 100 ms are IPv6, the chip could discard them before SPI. That is
   unverified: the next recorder must count IPv6 separately and capture source MACs.
6. **Logging: no detectable effect.** The no-subscriber and `esphome logs` runs compare as follows
   (306 lines delivered in 3 min):

   | | no subscriber | `esphome logs` |
   |---|---|---|
   | refused | 11 | 11 |
   | T2 late | 11 | 10 |
   | T3 early | 5 | 1 |
   | SPI-lock p99 | 231 µs | 238 µs |
   | Sn_CR retries | 0 | 0 |
   | replies off > 1 ms, per 1000 | 5.6 (4 events) | 11.3 (8 events) |
   | robust σ | 5.60 µs | 6.92 µs |

   The API frame rate rose from 12 to 22 per minute. The >1 ms difference is Poisson-compatible, and
   the σ difference comes from a single non-interleaved pair. Not tested: VERBOSE log level.
7. **New defect: stale receive-hook matches.** `hook_lookup_()` scans the 8-slot ring from index 0,
   returns the first key match, and checks no age. A request with a repeated transmit field (an
   all-zero field from some LAN client, and from the P4's priming packet) can match an entry minutes
   old, and that entry's T2 is served. HA `hook_latency` shows 11–574 s repeatedly, before *and*
   after the diagnostics flash. The E1 ring caught one case: the P4 priming reply was served with a
   T2 574 s old. Clients using T2 would see a delay of minutes and discard the sample; SNTP clients
   that read only T3 are unaffected. Fix candidates: newest match plus an age bound, and keying on
   source address and port as well.

### Phase 7 — Absolute reference (±1–5 µs, expectations set above)
- [ ] Identify a free GPIO on the board for PPS input (avoid RMII/PSRAM/flash pins).
- [ ] Wire GPS PPS → P4 GPIO **with a series resistor (~100 Ω) and a common ground wire**.
      Both boards are PoE-powered and therefore isolated; the ground wire establishes the
      shared reference. Keep the cable short.
- [ ] MCPWM capture (group 0, PLL160M → 6.25 ns) on the PPS edge.
- [ ] Correlate the MCPWM timebase to the PTP clock: paired reads with **alternating order**,
      plus back-to-back reads of each counter to measure and subtract the read skew.
- [ ] Publish the PTP-vs-GPS offset alongside each NTP quadruple.
- **Acceptance:** PTP-vs-GPS offset stable to ±1–5 µs; the residual θ against the S3 becomes
  attributable, testing the "T3 stamped ~124 µs early" hypothesis.

---

## Risks and Open Items

1. **Phase 3 is the gate.** If `ts4all` does not produce RX stamps for plain UDP, the design
   needs rethinking (fallback: newer ESP-IDF with `esp_eth_mac_enable_ts4all()`).
2. **`info` payload type** behind the RX callback — resolve by reading `esp_eth.c` at implementation.
3. **`ptp_start` may clear `en_ts4all`** — set it after, and re-verify after any link event.
4. **Free GPIO for PPS** — must be confirmed against the Waveshare pinout.
5. **PoE isolation** — the ground wire is required, not optional.
6. **IP101 PHY TX/RX latency asymmetry is uncharacterised** by anyone. It bounds absolute
   accuracy below ~1 µs and cannot be calibrated without a better reference. Affects Phase 7
   only; Phases 1–6 are unaffected because delay and differential θ do not depend on it.
7. **Rev 1.3 is confirmed** — no hardware PPS out. If a rev ≥ 3 board is ever obtained, Phase 7
   is replaced by a pure-hardware differential capture and should reach tens of nanoseconds.
